/*
 * page_select.c
 *
 *  Created on: Sep 19, 2024
 *      Author: user
 */

enum Page {
	SELECT_PRODUCT_LIST_PAGE,
	SELECT_PRODUCT_NORM_PAGE,
	MEASURING_PROCESS_PAGE
};

typedef uint8_t flag ;

typedef struct
{
	flag flag_select_product_list = 0;
	flag flag_select_product_norm = 0;
	//flag fla
};
enum Page current_page;

void page_select()
{
	switch(current_page)
	{
		case SELECT_PRODUCT_PAGE :

		break;

		case SELECTED_PRODUCT_NORM_PAGE :

		break;
	}
}
